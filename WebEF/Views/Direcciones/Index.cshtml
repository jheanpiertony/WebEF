@model IEnumerable<WebEF.Models.Direccion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StringDireccion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoDireccion)
            </th>
            <th>
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Actor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StringDireccion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoDireccion)
            </td>
            <td>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Actor.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
